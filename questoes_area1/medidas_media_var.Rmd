```{r data_generation, echo = FALSE, results = "hide"}
# Dados da questao

# Faca a resposta correta ser a primeira e as outras falsas

questions <- c("O desvio padrão de A é maior do que o de B, e as médias são iguais.",
               "O desvio padrão de A é menor do que o de B e as médias são diferentes.",
               "O desvio padrão de A é igual ao de B, independentemente do valor da média.",
               "As distribuições possuem o mesmo coeficiente de variação.",
               "O coeficiente de variação de B é maior do que o de A.")

solutions <- c(TRUE, rep(FALSE, 4))

o <- sample(1:5)
questions <- questions[o]
solutions <- solutions[o]

```

Question
========
Examinando a figura abaixo, é correto dizer:
\
```{r curvas, echo = FALSE, results = "hide", fig.height = 2, fig.width = 3.5, fig.path = "", fig.cap = ""}
par(mar = c(2.5, 2, 1, 0.5))
x <- seq(-4, 4, by = 0.01)
y1 <- dnorm(x, mean = 0, sd = 1) # B
df1 <- data.frame("x" = x, "y" = y1)

y2 <- dnorm(x, mean = 0, sd = 1.5) # A
df2 <- data.frame("x" = x, "y" = y2)

plot(x, y2, type = "l",
     lty = 2,
     ylim = c(0, max(y1) + 0.01),
     xlim = c(-4.5, 5),
     xlab = "",
     ylab = "",
     axes = FALSE)
lines(x, y1, lty = 1)
abline(v = 0, lty = 3)
arrows(x0 = -4.25, y0 = 0, x1 = 4.75, y1 = 0)
arrows(x0 = -4.25, y0 = 0, x1 = -4.25, y1 = max(y1) + 0.025)
legend("topright", lty = c(2, 1), legend = c("A", "B"), bty = "n")
axis(1, at = 0, labels = expression(bar(x)))
# axis(2, at = c(0, max(y2), max(y1)), labels = c("","",""))
```

```{r questionlist, echo = FALSE, results = "asis"}
answerlist(questions, markup = "markdown")
```

Solution
========
A média é uma medida de tendência central e pode ser interpretada como o ponto de equilíbrio da distribuição. Já o desvio padrão é uma medida de variabilidade ou dispersão e pode ser interpretado como um desvio típico com respeito à média da distribuição. Na questão temos duas distribuições com a mesma média, no entanto vemos que uma delas possui maior dispersão com respeito a este valor, enquanto que a outra é mais concentrada em torno da média. O desvio padrão captura esta informação: maior dispersão, maior é o desvio padrão; menor dispersão, menor é o desvio padrão.

```{r solutionlist, echo = FALSE, results = "asis"}
answerlist(ifelse(solutions, "Correta", "Incorreta"), markup = "markdown")
```

Meta-information
================
extype: schoice
exsolution: `r mchoice2string(solutions, single = TRUE)`
exname: medidadefinicoes