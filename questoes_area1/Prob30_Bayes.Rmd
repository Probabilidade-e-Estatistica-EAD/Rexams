```{r data_generation, echo = FALSE, results = "hide"}
## dados aleatorios da questao
# questions <- rep(0, 5)
# ## gerar ate que todas alternativas sejam diferentes
# while(length(unique(questions)) < 5 & any(diff(questions[order(questions)]) < 0.02)){
#   provas <- zeros <- 0
#   while(any(provas == 0)) provas <- c(rmultinom(1, 10, c(0.5, 0.3, 0.2))) / 10  # prob marginal
#   while(any(zeros == 0) & length(unique(zeros))!=3)
#         zeros <- c(rbinom(3, 20, 0.2)) / 20     # probs condicionais
#   conj <- provas * zeros       # probs conjuntas
#   total <- sum(conj)        # prob total
#   bayes <- conj / total
# 
#   ## se aleatorizar professor para pergunta
#   # prof <- c("A", "B", "C")  # identificacao dos professores
#   # por enquanto selecionamos o prof C
#   # profsol <- sample(1:3, 1) # professor selecionado para pergunta
# 
#   ## respostas
#   # sol <- conj[profsol] / total
#   sol <- conj[3] / total
#   err <- c(total, conj[sample(c(1,2,3)[-3], 1)], conj[3], provas[3])
# 
#   ## Faca a resposta correta ser a primeira e as outras falsas
#   questions <- c(sol, err)
# }

## resposta correta deve ser a primeira e as outras falsas
# questions <- format(round(questions, 2), nsmall = 2)      # respostas com aleatorizacao
questions <- c("0,18", "0,16", "0,33", "0,78", "0,80")    # respostas originais sem aleatorizacao
solutions <- c(TRUE, rep(FALSE, 4))

## aleatorizando o vetor de alternativas
o <- sample(1:5)
questions <- questions[o]
solutions <- solutions[o]


```

Question
========

Para corrigir a prova de redação do último vestibular, foram contratados três professores. Cada prova foi corrigida apenas por um professor. O professor $A$ corrigiu $50 \%$ das provas, o professor $B$ corrigiu $30 \%$ das provas e o professor $C$ corrigiu o restante. Sabe-se que $10 \%$ das provas corrigidas pelo professor $A$ obtiveram nota zero. O mesmo para o professor $B$, ou seja, $10 \%$ obtiveram nota zero. Já o professor $C$ atribuiu nota zero para $20 \%$ das provas que corrigiu. O primeiro colocado no vestibular não tirou zero na prova de redação. Qual a probabilidade de a redação dele ter sido corrigida pelo professor $C$?



```{r questionlist, echo = FALSE, results = "asis"}
answerlist(questions, markup = "markdown")
```

Solution
========

Considere:

Probabilidade de a redação ser corrigida pelo professor A = $P(A) = 0,5$

Probabilidade de a redação ser corrigida pelo professor B = $P(B) = 0,3$

Probabilidade de a redação ser corrigida pelo professor C = $P(C) = 0,2$

Probabilidade de não tirar zero na redação dado pelo professor A = $P(\bar{0}|A) = 0,9$

Probabilidade de não tirar zero na redação dado pelo professor B = $P(\bar{0}|B) = 0,9$

Probabilidade de não tirar zero na redação dado pelo professor C = $P(\bar{0}|C) = 0,8$

O que queremos é:

$P(C|\bar{0}) = \frac{P(\bar{0} \cap C)}{P(C)} =  \frac{P(C)*P(\bar{0}|C)} {P(A)*P(\bar{0}|A)+P(B)*P(\bar{0}|B)+P(C)*P(\bar{0}|C)}$


$P(A|I)  = \frac{(0,2*0,8)}{(0,5*0,9)+(0,3*0,9)+(0,2*0,8)} = \frac{0,16}{0,88} = 0,18$


```{r solutionlist, echo = FALSE, results = "asis"}
answerlist(ifelse(solutions, "Correta", "Incorreta"), markup = "markdown")
```


Meta-information
================
extype: schoice
exsolution: `r mchoice2string(solutions, single = TRUE)`
exname: conceitos basicos